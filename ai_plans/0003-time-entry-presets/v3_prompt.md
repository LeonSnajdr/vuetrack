Also adjsut the plan with that:
- The presets should be saved in the a new presetStore
- The presets should for now just be saved in memory in a ref in this store
- The store returns also the active preset
