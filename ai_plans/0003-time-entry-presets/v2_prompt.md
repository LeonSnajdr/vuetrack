please adjust the plan with these feedbacks:
 - the presets should use a VNumberInput for the duration
-  The TimeEntryPreset should be located in the /models directory.
- since the startTime in the end always exist add the duration just ontop of the startTime
